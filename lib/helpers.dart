import 'package:flutter/material.dart';

// Colors
Color appDarkGreyColor = Color.fromRGBO(58, 66, 86, 1.0);
Color appGreyColor = Color.fromRGBO(88, 92, 110, .9);

Color DarkGreen = Color.fromRGBO(2, 105, 114, 1.0);
Color lightGreen = Color.fromRGBO(124, 189, 184, 1.0);
Color lightBlue = Color.fromRGBO(234, 245, 249, 1.0);
Color Orange = Color.fromRGBO(217, 145, 118, 1.0);
Color RiceWhite = Color.fromRGBO(242, 237, 228, 1.0);

// Strings
const appTitle = "";
const pinCodeHintText = "Pin Code";
const loginButtonText = "Login";

// Images
Image appLogo = Image.asset('lib/image/test_Logo.png');

// Sizes
const bigRadius = 120.0;
const buttonHeight = 25.0;

// Pages
const loginPageTag = 'Login Page';
const homePageTag = 'Home Page';
